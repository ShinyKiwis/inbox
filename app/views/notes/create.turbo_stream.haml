= turbo_stream.append :page_content do
  = render 'note', note: @note
= turbo_stream.append :notebook_content do
  = note_item @notebook, @note
  :javascript
    Turbo.visit('#{notebook_note_path(@notebook, @note)}')

