- if resource.errors.any?
  #error_explanation{"data-turbo-cache" => "false"}
    %p.prompt.error You are missing something:
    %ul
      - resource.errors.full_messages.each do |message|
        %li.error= message
